[tool:pytest]
markers =
    e2e: marks tests as end-to-end integration tests
    unit: marks tests as unit tests
    slow: marks tests as slow running
    ground_truth: marks tests as ground truth comparison (requires ESTIMAI_SEED=42)
    consistency: marks tests as determinism/consistency checks (10x runs)
    benchmark: marks tests as performance/accuracy benchmarks (logs to LangSmith)
testpaths = backend/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short